name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  DJANGO_SETTINGS_MODULE: core.settings.settings_test
  SECRET_KEY: ci-test-secret-key-for-automated-checks-2026-09
  JWT_SIGNING_KEY: ci-test-jwt-signing-key-for-automated-checks-2026-09
  ALLOWED_HOSTS: testserver,localhost,127.0.0.1
  CORS_ALLOWED_ORIGINS: http://localhost:3000
  CSRF_TRUSTED_ORIGINS: http://localhost:3000
  DB_NAME: erp_test
  DB_USER: erp
  DB_PASSWORD: erp
  DB_HOST: 127.0.0.1
  DB_PORT: 5432
  REDIS_URL: redis://localhost:6379/0

jobs:
  checks:
    name: Static, Django and production checks
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: backend
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_DB: erp_ci
          POSTGRES_USER: erp
          POSTGRES_PASSWORD: erp
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U erp -d erp_ci"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip check
          python -m pip install pip-audit bandit

      - name: Verify synchronous-only runtime
        shell: bash
        run: |
          set -euo pipefail

          if find . -type f -path '*async*' -not -path './.git/*' -print -quit | grep -q .; then
            echo "Async-named files are not allowed on main."
            find . -type f -path '*async*' -not -path './.git/*' -print
            exit 1
          fi

          if [[ -e core/asgi.py ]]; then
            echo "core/asgi.py must not exist on the synchronous main branch."
            exit 1
          fi

          if grep -RInE --exclude-dir=.git --include='*.py' \
            -e '(^|[[:space:]])from[[:space:]]+adrf([.]|[[:space:]])' \
            -e '(^|[[:space:]])import[[:space:]]+adrf([.]|[[:space:]])' \
            -e 'sync_to_async' \
            -e '(^|[[:space:]])async[[:space:]]+def[[:space:]]' \
            .; then
            echo "Async-only code was found on the synchronous main branch."
            exit 1
          fi

      - name: Verify migration files
        run: python manage.py makemigrations --check --dry-run

      - name: Run Django checks
        run: python manage.py check

      - name: Production Django checks
        env:
          DJANGO_SETTINGS_MODULE: core.settings.settings_prod
          DB_NAME: erp_ci
          DB_USER: erp
          DB_PASSWORD: erp
          DB_HOST: 127.0.0.1
          DB_PORT: 5432
          REDIS_URL: redis://127.0.0.1:6379/0
          ALLOWED_HOSTS: example.com
          CORS_ALLOWED_ORIGINS: https://example.com
          CSRF_TRUSTED_ORIGINS: https://example.com
          AWS_ACCESS_KEY_ID: ""
          AWS_SECRET_ACCESS_KEY: ""
          AWS_STORAGE_BUCKET_NAME: ""
        run: python manage.py check --deploy --tag security --fail-level ERROR

  tests:
    name: Tests / ${{ matrix.suite }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: backend
    strategy:
      fail-fast: false
      max-parallel: 3
      matrix:
        include:
          - suite: authentication
            apps: accounts authsession authentication
          - suite: organization
            apps: organization customers customer_assignments suppliers
          - suite: catalog
            apps: products coupons
          - suite: sales
            apps: invoices payments inventory
          - suite: purchasing
            apps: purchases
          - suite: accounting
            apps: accounting auditlog
          - suite: infrastructure
            apps: core common

    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_DB: erp_test
          POSTGRES_USER: erp
          POSTGRES_PASSWORD: erp
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U erp -d erp_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip check

      - name: Run ${{ matrix.suite }} tests
        run: python manage.py test ${{ matrix.apps }}

  frontend:
    name: Frontend build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Audit frontend dependencies
        run: npm audit --audit-level=high

      - name: Typecheck frontend
        run: npm run typecheck

      - name: Build frontend
        run: npm run build

      - name: Build production frontend image
        run: docker build -t erp-api-frontend:ci .

  security:
    name: Dependency and source security
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: backend
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install pip-audit bandit

      - name: Audit Python dependencies
        run: pip-audit -r requirements.txt --strict

      - name: Scan Python source
        run: bandit -q -r . -x tests -lll
