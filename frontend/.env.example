# Browser-facing API base URL for the Vite development server/build.
VITE_API_URL=/api/v1

# Vite dev-server proxy target only; not used by the production Nginx image.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
